# Force git to treat source/config files as text, even if they pick up stray
# NUL bytes from the VM filesystem. This prevents git from misclassifying them
# as binary blobs, which suppresses textual diffs and corrupts the object store.

# Snakemake workflow files
Snakefile        text eol=lf
*.smk            text eol=lf

# Python
*.py             text eol=lf

# Config / data
*.yaml           text eol=lf
*.yml            text eol=lf
*.toml           text eol=lf
*.cfg            text eol=lf
*.csv            text eol=lf
*.json           text eol=lf
*.md             text eol=lf

# Jupyter notebooks: text (like JSON) plus the nbstripout clean filter, which
# strips cell outputs/counts from the stored blob so notebooks stay small.
# The filter itself is per-clone — run `nbstripout --install` once (see
# notebooks/README.md); without it the entry is a harmless no-op.
*.ipynb          text eol=lf filter=nbstripout
*.txt            text eol=lf
*.sh             text eol=lf

# Explicitly mark true binary types so git doesn't try to diff them as text
*.parquet        binary
*.feather        binary
*.nc             binary
*.png            binary
*.jpg            binary
*.jpeg           binary
*.gif            binary
*.ico            binary
*.woff           binary
*.woff2          binary
*.zip            binary
*.shp            binary
*.dbf            binary
*.shx            binary
*.xlsx           binary
