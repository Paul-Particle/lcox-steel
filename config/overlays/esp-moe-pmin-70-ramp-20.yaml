# Overlay for the `esp-moe-pmin-70-ramp-20` scenario, deep-merged over assumptions.yaml.
#
# MOE cell with minimum load 0.7, held to 0.20 of nameplate per hour.
#
# Part of testrun 2.2; see scratch/add_testrun22_scenarios.py.
moe:
  p_min_pu: 0.7
  ramp_limit_up: 0.2
  ramp_limit_down: 0.2
