# Overlay for the `esp-tank-ramp-20-grid` scenario, deep-merged over assumptions.yaml.
#
# Tank storage; electrolyser and H2 shaft each held to 0.20 of nameplate per hour.
#
# Part of testrun 2.2; see scratch/add_testrun22_scenarios.py.
h2_buffer:
  capex_per_mwh_eur: 60050
electrolyser:
  ramp_limit_up: 0.2
  ramp_limit_down: 0.2
dri-h2:
  ramp_limit_up: 0.2
  ramp_limit_down: 0.2
