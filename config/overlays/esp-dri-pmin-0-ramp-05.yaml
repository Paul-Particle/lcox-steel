# Overlay for the `esp-dri-pmin-0-ramp-05` scenario, deep-merged over assumptions.yaml.
#
# H2 shaft with no minimum load, held to 0.05 of nameplate per hour.
#
# Part of testrun 2.2; see scratch/add_testrun22_scenarios.py.
dri-h2:
  p_min_pu: 0.0
  ramp_limit_up: 0.05
  ramp_limit_down: 0.05
