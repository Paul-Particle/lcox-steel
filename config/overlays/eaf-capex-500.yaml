# Overlay for the `eaf-capex-500` scenario, deep-merged over assumptions.yaml.
#
# One input moved and nothing else: a dearer furnace than the register's.
#
# Part of the overnight sensitivity set. Routes are limited to the ones this
# input can reach, so a run that could not respond to it is not solved at all.
eaf:
  capex_per_t_per_year_eur: 500
