# Overlay for the `eaf-capex-200` scenario, deep-merged over assumptions.yaml.
#
# One input moved and nothing else: a cheaper furnace than the register's.
#
# Part of the overnight sensitivity set. Routes are limited to the ones this
# input can reach, so a run that could not respond to it is not solved at all.
eaf:
  capex_per_t_per_year_eur: 200
