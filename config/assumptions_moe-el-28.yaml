# Overlay for the `moe-el-28` scenario, deep-merged over assumptions.yaml.
#
# One input moved and nothing else: a more efficient cell than Boston Metal's public estimate.
#
# Part of the overnight sensitivity set. Routes are limited to the ones this
# input can reach, so a run that could not respond to it is not solved at all.
moe:
  el_mwh_per_t: 2.8
